# ========================================
# 化学物质猜测测试 - 客户端环境变量配置
# Chemistry Guessr Client - Environment Variables
# ========================================

# ----- API 配置 (API Configuration) -----
# 后端 API 地址
# 本地开发: http://localhost:3000
# 生产环境: https://your-backend-domain.com
VITE_SERVER_URL=http://localhost:3000

# Socket.IO 服务器地址（通常与 API 地址相同）
VITE_SOCKET_URL=http://localhost:3000


# ----- 安全配置 (Security Configuration) -----
# AES 加密密钥（用于本地数据加密）
# 生成方法: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
VITE_AES_SECRET=my-secret-key


# ----- 应用配置 (Application Configuration) -----
# 应用标题
VITE_APP_TITLE=化学测试 & 资料库

# 应用描述
VITE_APP_DESCRIPTION=通过属性猜测化学物质，学习高中化学知识

# 应用版本
VITE_APP_VERSION=1.0.0


# ----- 功能开关 (Feature Flags) -----
# 是否启用排行榜功能
VITE_ENABLE_LEADERBOARD=true

# 是否启用测试记录功能
VITE_ENABLE_TEST_RECORDS=true

# 是否启用资料库功能
VITE_ENABLE_LIBRARY=true


# ----- 化学测试模式 (Chemistry Test Mode) -----
# 是否使用本地化学数据（无需后端服务器）
# true: 使用前端本地数据，不依赖后端
# false 或未设置: 优先使用后端 API，失败时自动降级到本地模式
VITE_USE_LOCAL_CHEMISTRY=false


# ----- 开发环境配置 (Development Only) -----
# 是否启用调试模式
VITE_DEBUG_MODE=false

# 是否显示控制台日志
VITE_ENABLE_CONSOLE_LOGS=true